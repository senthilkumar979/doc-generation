# DocRail 0.0.3

## For users

- Supabase **publishable** API keys are supported via `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (dashboard default for new projects).
- The legacy **`NEXT_PUBLIC_SUPABASE_ANON_KEY`** JWT still works if both are not migrated yet.
